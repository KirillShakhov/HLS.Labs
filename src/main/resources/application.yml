spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
eureka:
  instance:
    lease-renewal-interval-in-second: 3
server:
  port: 8888

